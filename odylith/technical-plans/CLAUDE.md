# CLAUDE.md

@AGENTS.md

## Claude Code

- This scoped file ensures Claude loads the same technical-plan subtree contract as other hosts.
- Prefer `/odylith-plan` when you need a bounded plan for an active slice, and keep the plan tied to the same workstream, component, or bug that is actually moving.
- Treat plan markdown here as execution truth: keep checklists, validation obligations, and completion evidence aligned with implementation instead of forking speculative side plans.
- For broader Odylith context outside this subtree, follow `odylith/AGENTS.md` and the repo-root bridge.
- Do not treat this file as an implementation plan record; it is only the Claude companion for this scope.
