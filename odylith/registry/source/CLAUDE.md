# CLAUDE.md

@AGENTS.md

## Claude Code

- This scoped file ensures Claude loads the same Registry subtree contract as other hosts.
- Prefer the `odylith-registry-scribe` subagent or the registry-spec-sync skill shim when the slice is component dossier or registry contract work.
- Treat `components/*/CURRENT_SPEC.md` plus the registry source manifests here as canonical. Keep rendered registry output aligned, but do not author changes in the generated surface.
- For broader Odylith context outside this subtree, follow `odylith/AGENTS.md` and the repo-root bridge.
- Do not treat this file as a component registry record; it is only the Claude companion for this scope.
